{
  "name": "glitch",
  "module": "src/main.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build --env-file prod.env ./examples/simple.html --minify --outdir=dist",
    "watch": "bun --env-file dev.env ./server.ts"
  }
}